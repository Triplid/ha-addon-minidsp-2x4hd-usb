name: "MiniDSP 2x4 HD USB (Presets & Source)"
description: "USB control for miniDSP 2x4 HD: read presets and source via HID"
version: "1.0.3"  # Увеличиваем для пересборки
slug: minidsp_2x4hd_usb
url: "https://github.com/Triplid/ha-addon-minidsp-2x4hd-usb"
arch:
  - aarch64  # Только для HA Green
startup: application
boot: auto
map:
  - share:rw
devices:
  - /dev/hidraw*:/dev/hidraw*:rwm
  - /dev/bus/usb:/dev/bus/usb:rwm
privileged: true  # Правильный формат для USB-доступа (не массив)
homeassistant_api: true
schema: {}
